// Stub â€” Phase 3 builds the real dashboard with Skia charts.
import { Text, View } from 'react-native';

export default function Dashboard() {
  return (
    <View className="flex-1 items-center justify-center bg-deep">
      <Text className="text-cream font-display font-black" style={{ fontSize: 48 }}>
        DASHBOARD
      </Text>
    </View>
  );
}
